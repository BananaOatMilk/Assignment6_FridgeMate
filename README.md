# Assignment6_FridgeMate

Initial backbone implementation for FridgeMate:
- domain model: `FoodItem`, `FoodCategory`, `StorageLocation`
- inventory service: `Inventory`
- strategy abstraction: `ExpirationPolicy`, `DefaultExpirationPolicy`
- unit tests for inventory and validation

## Run tests

```bash
mvn test
```
